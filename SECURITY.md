# Security

Do not commit API keys, access tokens, email addresses, or private input images.

Use environment variables or an interactive credential helper for GitHub authentication. If a credential is ever committed to a notebook or script, revoke it immediately and remove it from the published artifact before sharing.
